version: '3.2'

services:
  biobot:
    image: biobot
    build: .
    container_name: biobot
    env_file:
      - env
    environment:
      - TIME_ZONE=Europe/Minsk
      - "BIO_PATTERN=my local time is {TIME}"
    volumes:
      - ./bio.session:/app/bio.session
